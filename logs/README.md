# Logs

Build and test run logs for this project.

- `make-test.log` - output of `make test` (nodeenv bootstrap + `jlpm test` / jest)
- `tsc-check.log` - TypeScript compile check of the frontend (`src/`)
